// Runtime API URL (loaded before the app). Edit this on the VPS so the frontend
// talks to your backend without rebuilding. Use your VPS IP or domain and backend port.
// Example: window.__API_BASE_URL__ = 'http://75.119.146.174:8080/api/v1';
window.__API_BASE_URL__ = '';
